How did viewing a diff between two versions of a file help you see the bug that was introduced?
On Mac, diff -u showed changes between files

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?
Easier to revert changes or see differences?

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git vs having versions automaatically saved, like Google Docs does?
More logical break points

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file seperately?
Easier for Google to track or restore? Less disk space?

How can you use the commands git log and git diff to view the history of files?
git log to see the commits, git diff to see the changes between commits

How might using version control make you more confident to make changes that could break something?
If you do break something, you'll only break a small portion, so you won't lose as much time. 

Now that you have your workspace setup, what do you want to try using Git for?
Creating a repository for Udacity